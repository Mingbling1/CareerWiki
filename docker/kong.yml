_format_version: "1.1"

services:
  # ─── GoTrue Auth ────────────────────────────────────────
  - name: auth-v1
    url: http://empliq-supabase-auth:9999/
    routes:
      - name: auth-v1-route
        strip_path: true
        paths:
          - /auth/v1/
    plugins:
      - name: cors

  # ─── PostgREST (placeholder — not used yet) ────────────
  # - name: rest-v1
  #   url: http://empliq-postgrest:3000/
  #   routes:
  #     - name: rest-v1-route
  #       strip_path: true
  #       paths:
  #         - /rest/v1/
